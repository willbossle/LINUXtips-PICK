apiVersion: v1
kind: Service
metadata:
  name: {{ include "giropops-senhas-charts.fullname" . }}
  labels:
    {{- include "giropops-senhas-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "giropops-senhas-charts.selectorLabels" . | nindent 4 }}
